[project]
name = "connect-to-ai-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Mazen", email = "Mazen-Muhamed@outlook.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
connect-to-ai-api = "connect_to_ai_api:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
